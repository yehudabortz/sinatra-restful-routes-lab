<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body { font-family: Helvetica;}
        div { 
            padding: 25px;
            background-color: aliceblue;
            margin: 35px;
            }
            a {
                color: darkblue;
            text-decoration: none;
            }
    </style>
</head>
<body>
    <button><a href='/recipes/<%= "#{@recipe.id}" %>/edit'>Edit</a></button>
    <div>
        <h2><%= @recipe.name %></h2>
        <p><%= @recipe.ingredients %></p>
        <strong><p><%= @recipe.cook_time %></p></strong>
        <form id="delete" method="POST" action='/recipes/<%= "#{@recipe.id}" %>'>
            <input id="hidden" type="hidden" value="delete" name="_method">
            <input type="submit" value="delete">
        </form>
    </div>
</body>
</html>